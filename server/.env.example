# Discord OAuth Configuration (Required for authentication)
DISCORD_CLIENT_ID=your_discord_client_id_here
DISCORD_CLIENT_SECRET=your_discord_client_secret_here
DISCORD_REDIRECT_URI=http://localhost:3001/auth/discord/callback

# Server Configuration
PORT=3001
# Note: Frontend Vite dev server runs on port 3000 by default (configured in vite.config.mjs)
FRONTEND_URL=http://localhost:3000

# ===========================================
# DISCORD BOT COMMUNICATION CONFIGURATION
# ===========================================
# Required for backend to communicate with Discord bot via Discord API

# Discord Bot Token (Bot permissions: Send Messages, Read Message History)
DISCORD_BOT_TOKEN=your_bot_token_here

# Discord Server and Channel IDs
DISCORD_GUILD_ID=your_discord_server_id
DISCORD_ADMIN_CHANNEL_ID=your_admin_channel_id

# RECOMMENDED: Webhook URL for sending commands (appears as human messages)
# This solves the issue where RedBot ignores messages from other bots
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/your_webhook_id/your_webhook_token

# Command prefix for bot commands (default: !cw)
COMMAND_PREFIX=!cw

# ===========================================
# BOT SETUP INSTRUCTIONS
# ===========================================
# 1. Create a private admin channel in your Discord server
# 2. Add the bot to this channel with appropriate permissions
# 3. Set DISCORD_ADMIN_CHANNEL_ID to this channel's ID
# 4. Backend will send commands to this channel to control the bot
# 
# Cog <-> Backend Shared Secret
# A shared secret used by the CollabWarz cog and the backend to authenticate HTTP calls
# from the cog. Set this to a strong random value. The cog's `backend_token` must match
# the `COLLABWARZ_TOKEN` environment variable on the backend for log/action postings.
COLLABWARZ_TOKEN=your_shared_secret_here

# No additional environment variables are needed for the bot.

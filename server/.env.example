# Discord OAuth Configuration (Required for authentication)
DISCORD_CLIENT_ID=your_discord_client_id_here
DISCORD_CLIENT_SECRET=your_discord_client_secret_here
DISCORD_REDIRECT_URI=http://localhost:3001/auth/discord/callback

# Server Configuration
PORT=3001
# Note: Frontend Vite dev server runs on port 3000 by default (configured in vite.config.mjs)
FRONTEND_URL=http://localhost:3000

# ===========================================
# DISCORD BOT CONFIGURATION
# ===========================================
# The Discord bot (RedBot cog) uses its own internal configuration system.
# Bot configuration is managed via Discord commands:
#   [p]cw setchannel #channel-name     - Set announcement channel
#   [p]cw setaiconfig <url> <key>      - Configure AI service (optional)
#   [p]cw biweekly                     - Enable bi-weekly mode
#   [p]cw status                       - View current configuration
# 
# No additional environment variables are needed for the bot.
